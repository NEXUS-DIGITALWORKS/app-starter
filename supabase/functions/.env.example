# Supabase Edge Functions用のローカル環境変数（`supabase functions serve`が読み込む）。
# 本番はリポジトリに含めず `supabase secrets set ANTHROPIC_API_KEY=...` で登録する。
# ローカル検証時は、このファイルを同じディレクトリに `.env` としてコピーして値を設定する。
ANTHROPIC_API_KEY=sk-ant-your-api-key
